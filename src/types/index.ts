export type ProductCardValue = {
  id: number;
  name: string;
  quantity: number;
  price: number;
  description: string;
  photo: string;
};

export type CustomerData = {
  firstName: string;
  address: string;
  phoneNumber: string;
};
